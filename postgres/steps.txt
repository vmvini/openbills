#Construir imagem
sudo docker build -t pg-eleicoes .

#rExecutar imagem criada
sudo docker run -it -d -v SUA_PASTA_LOCAL_DE_DADOS:/var/dados pg-eleicoes
#exemplo:
sudo docker run -it -d -v /home/vmvini/Dropbox/ADS/SEXTO_PERIODO/POS/projetos/projeto1/dados:/var/dados pg-eleicoes


#Usar o "terminal" da imagem criada:
sudo docker exec -it CODIGO_DO_CONTAINER_EM_EXECUCAO /bin/bash

#importe os arquivos csv para as tabelas do banco com o comando:
node importer.js


#Para visualizar o banco com os novos dados:
#selecionar banco
psql -U postgres eleicoes

#Com esse bash do psql, é possível fazer qualquer coisa que vc faria com a ferramenta pgAdmin.






#----COMANDOS UTILITÁRIOS-------------
#show database encoding 
psql -U postgres eleicoes -c 'SHOW SERVER_ENCODING'
#OR 
;SHOW SERVER_ENCODING; 

#check csv file encoding through linux bash
file fileName.csv


#TROCAR VIRGULA POR PONTO NOS NUMEROS
sed s/','/'.'/g ReceitaCandidato.csv > ReceitaCandidatoDecimalPointFix.csv
